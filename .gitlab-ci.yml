image: maven:3.8.4-openjdk-17

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn clean test

deploy:
  stage: deploy
  script:
    - mvn spring-boot:run

before_script:
  - mvn --version